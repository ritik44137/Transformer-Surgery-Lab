# Baseline training hyperparameters for the classical stack

train:
  max_steps: 2000
  batch_size: 32
  eval_interval: 200
  log_interval: 50
  learning_rate: 3.0e-4
  weight_decay: 0.1
  warmup_steps: 100
  grad_clip: 1.0
  scheduler: cosine
  min_lr_ratio: 0.1
  eval_max_batches: null
